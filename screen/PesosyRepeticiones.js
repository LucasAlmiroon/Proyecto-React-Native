import { FlatList, StyleSheet, Text, View } from 'react-native'
import { DropdownComponent } from '../components/DropdownComponent'


const PesosyRepeticiones = ({ }) => {
    return (
        <DropdownComponent />
    )
}

export default PesosyRepeticiones

const styles = StyleSheet.create({
    container: {
        textAlign: "center"
    }
})